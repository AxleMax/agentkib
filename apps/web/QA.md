# Web v1 前端验收（2026-09-08）

## 范围与隔离

读取 `pen-design` 技能及 v14 `.pen` 24–31 页节点、总览 PNG，以其配对、等待、阅读、工具与审批层级实现；未修改或生成设计稿。按 Playwright 技能使用命名浏览器 `web-ui`，访问 `node apps/web/scripts/qa-server.mjs` 在 `127.0.0.1:1423` 提供的 **构建后 dist 静态资源**。脚本完全合成，不调用 runtime、IPC 或任何真实 Agent；不进入安装包。

## 已验证

- `pnpm --filter @agentkib/web test`：18 项通过。覆盖可靠轮次折叠、未分类与不完整正文、失败工具、分页展开选择、Markdown 外链/远程图片安全、CSRF 与不重试、撤销与索引关闭清空、断线控制禁用、owner 决定列表及 cwd 展示。
- `pnpm --filter @agentkib/web typecheck`、`build`、`git diff --check` 通过。Web 构建包含本地 Geist 字体、favicon 与 `build-info.json`（version / revision / dirty，无本机路径和时间戳）。
- Chromium 实际窗口 390×844、768×1024、1360×860、1440×920，阅读页面四尺寸深浅各截图并逐张查看：无页面横向溢出、900px 最大阅读列、长代码独立横滚、手机目录/阅读切换、底部输入区不挤出屏幕。
- 配对输入不足 8 位时不可提交；填写后进入校验数字等待页，再由合成 owner 状态推进到目录。
- 执行过程按钮鼠标展开与 Enter 收起/展开正常；明确最终回复始终可见。工具详情弹窗可读长输出，Escape 关闭后焦点回到原 exec 按钮。
- 加载更早记录前用户消息顶部为 253px，加载后为 253.09375px；展开选择保留。
- 合成发送后显示“请求已接收，不代表执行完成”；生成数值 ID `42` 的合成审批，展示完整命令与 `/tmp` 目录，拒绝后审批消失、状态回空闲。没有执行真实命令。
- 初次 QA 发现 favicon 404，补充本地 SVG 后重新构建/加载，页面无新资源错误。
- 日文切换后 UI 文案正常（合成记录正文仍保持原语言）；浏览器模拟离线后显示断线提示，输入及发送禁用。离线产生的网络错误属于故障注入预期，非运行时 JS 异常。浏览器恢复在线后关闭，合成 QA 服务已停止。

截图位于仓库 `output/playwright/web-v1/`：pair/pending、reader 四尺寸暗浅、tool 暗浅、approval 与 approval-result。

## 未覆盖及限制

- 上述审批与发送仅为合成 HTTP UI 验证，不能证明真实 Codex 执行通过。正式服务的实验控制应继续受主机验收门槛限制。
- 未使用实际手机，未验证 iOS/Android 软键盘、Safari、Firefox；桌面 viewport 不是这些测试的替代。
- HTTPS 反向代理、真实配对/撤销、打包桌面服务生命周期由主代理另行记录；本记录不宣称这些已通过。
- 不包含停止/中断接口或按钮，未向用户原测试会话发送并发请求。
